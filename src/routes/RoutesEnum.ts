export enum RoutesEnum {
  Root = "/",
  Home = "/home",
  VideoPlayer = "/video/:id",
  MusicPlayer = "/music/:id",
  Account = "/account",
  Favorites = "/favorites",
  MyContent = "/my-content",
}
