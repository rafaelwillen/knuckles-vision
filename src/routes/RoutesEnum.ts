export enum RoutesEnum {
  Root = "/",
  Login = "/login",
  SignUp = "/sign-up",
  Home = "/home",
  VideoPlayer = "/video/:id",
  MusicPlayer = "/music/:id",
  Account = "/account",
  Favorites = "/favorites",
  Radio = "/radio",
  CreateGroup = "/group/new",
}
